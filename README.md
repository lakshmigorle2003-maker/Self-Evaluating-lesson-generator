# Self-Evaluating-lesson-generator
Self-evaluating lesson generator: an LLM writes a beginner lesson, a second LLM grades it against a hard 13-point pass/fail rubric, and it auto-regenerates until it passes or retries run out.
